#include <stdio.h>

/**
 * main - entry point
 *
 * Description: prints the alphabet, in lowercase, followed by a new line.
 *
 * Return: 0=success
 */

int main(void)
{
	int n;

	for (n = 'a'; n <= 'z'; n++)
		_putchar(n);
	_putchar('\n');


return (0);
}
